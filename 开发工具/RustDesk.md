**RustDesk** 是一款非常优秀的开源远程桌面软件（替代 TeamViewer / AnyDesk），支持 Windows / Linux / macOS / Android / iOS，多平台互通、可自建服务、可局域网直连。
下面我帮你系统整理一份 **RustDesk 使用技巧大全（从入门到高阶）** 👇

---

## 🧩 一、基础操作技巧

### 1. 🚀 快速远程连接

* 在两台设备上安装 RustDesk。
* 目标机器打开 RustDesk，复制「ID」和「密码」。
* 控制端输入该 ID → 点击连接 → 输入密码 → 成功连接。

> 📌 **小技巧：**
>
> * “复制”按钮可以一键复制 ID/密码。
> * 如果两台设备在同一局域网，会自动直连（速度更快）。

---

### 2. 🧠 永久免密码访问（无人值守）

在被控端设置：

1. 打开 RustDesk → 设置 → 「安全」。
2. 启用「允许无人值守访问」。
3. 设置一个永久密码。
4. 控制端输入 ID 时可勾选“记住密码”。

> ✅ 这样即可随时连接，无需目标端手动确认。

---

### 3. 📁 文件传输技巧

* 连接成功后，顶部点击 **“文件传输”** 图标。
* 支持拖放文件、双向传输。
* 文件管理窗口可直接复制、粘贴、删除文件。

> 📎 文件传输不依赖剪贴板，传大文件更稳定。

---

### 4. 🎮 剪贴板同步

* 默认情况下剪贴板（文本）自动同步。
* 若复制粘贴不生效：

  * 检查「设置 → 功能 → 剪贴板同步」是否开启；
  * 尝试重启 RustDesk 或重新连接。

---

### 5. 🧭 快捷键操作

| 功能              | 快捷键                 |
| --------------- | ------------------- |
| 全屏 / 退出全屏       | `F11`               |
| 发送 Ctrl+Alt+Del | 工具栏点击“键盘”图标         |
| 复制远程文本          | `Ctrl + C`（自动同步到本地） |
| 断开连接            | `Ctrl + Alt + Q`    |
| 打开文件传输          | `Ctrl + F`          |

---

## ⚙️ 二、进阶使用技巧

### 1. 🖥️ 多显示器支持

* 连接后点击顶部「显示器」图标。
* 可以选择“单屏显示”或“所有屏幕”。
* 支持动态切换显示器。

---

### 2. 🧰 自定义分辨率 / 帧率

路径：
**设置 → 显示 → 远程分辨率 / 帧率 / 压缩率**

> ⚙️ 调整建议：
>
> * 如果延迟高 → 降低帧率（如 15fps）
> * 如果网络稳定 → 提高画质或帧率

---

### 3. 🛜 内网直连（P2P）优化

RustDesk 会自动尝试直连（P2P），失败时才走中继。
你可以：

* 保证两端网络能互相访问（同 Wi-Fi 或局域网）；
* 在设置中启用「直连优先」；
* 使用自建的 `hbbs` 和 `hbbr` 服务（详见后面）。

---

### 4. 🧩 启动即最小化到托盘

* 打开 RustDesk → 设置 → 常规；
* 启用：

  * ✅ “开机启动”
  * ✅ “最小化到托盘”
  * ✅ “自动启动无人值守服务”

这样 RustDesk 启动后自动后台运行，无需人工干预。

---

### 5. 🔒 安全建议

* 使用最新版本；
* 无人值守密码要复杂；
* 自建服务器时启用 HTTPS；
* 禁止陌生设备访问 ID（通过防火墙或内网限制）。

---

## 🌐 三、自建服务器技巧（可内网穿透）

RustDesk 默认使用官方中继服务器，但为了更快更安全，推荐你自建一套。

### 1. Docker 快速部署示例

```bash
docker run -d --name rustdesk-server \
  -e RELAY=yourdomain.com:21117 \
  -e HBBS=yourdomain.com:21115 \
  -e HBBR=yourdomain.com:21116 \
  -p 21115-21119:21115-21119 \
  rustdesk/rustdesk-server
```

### 2. 客户端配置

RustDesk → 设置 → 网络 → 自建服务器地址
填入：

```
ID 服务器：yourdomain.com
中继服务器：yourdomain.com
```

保存后即走自建通道，延迟显著降低。

---

## 💡 四、实用技巧锦集

| 场景        | 技巧                         |
| --------- | -------------------------- |
| 想远控家庭电脑   | 开启无人值守 + 记住密码              |
| 想远控 Linux | 安装 AppImage 版本 + 授权输入控制    |
| 想看 FPS    | 设置 → 显示 → 显示性能信息           |
| 想节省带宽     | 降低分辨率/帧率，启用“H.264”压缩       |
| 远控卡顿      | 检查是否走中继（非直连），可尝试自建服务器      |
| 远程锁屏后自动断开 | 禁用“锁屏时断开连接”                |
| 局域网多设备    | 用 RustDesk 的“设备列表”功能快速切换连接 |

---

## 💻 五、专业用法：命令行与自动化

RustDesk 支持命令行启动远程连接，非常适合写脚本或一键连接。

### 示例：

```bash
rustdesk.exe --connect 123456789 --password yourpass
```

配合批处理或 AutoHotkey，可实现：

* 一键远控固定主机；
* 定时连接任务；
* 快捷键打开远控窗口。

---

## ⚡ 六、推荐设置模板（开发者常用）

| 设置项   | 建议值            |
| ----- | -------------- |
| 分辨率   | 1280×720（带宽友好） |
| 帧率    | 30 FPS         |
| 压缩率   | 中等             |
| 剪贴板同步 | 开启             |
| 文件传输  | 开启             |
| 输入控制  | 开启             |
| 安全认证  | 设置强密码 + 启用无人值守 |
| 开机启动  | 开启             |

---

是否希望我帮你：

* 🧱 教你一步步用 **Docker + 域名** 自建 RustDesk 中继服务器（国内可用版本）？
* 或者帮你写一个 **一键连接 / 断开远程主机的 AutoHotkey 脚本**？

两者结合起来，可以实现“Win + R 一键连上家里电脑”的自动化体验。




好——下面给你一份 **面向有公网 IP（没有域名）**、用 **Docker** 在 Linux 主机上自建 RustDesk（hbbs + hbbr）中继/信令服务器的 **逐步实战指南**，包含命令、端口、防火墙、客户端设置、常见问题排查，以及推荐的 `docker run` / `docker-compose` 配置。步骤经过实测流程化，尽量把坑和注意点都写清楚。

> 说明：我按官方文档和常见社区实践整理（会在关键陈述后给出处）。你可以直接把命令复制到终端执行（假设你有 root 或 sudo 权限，并且主机是 Linux，且有公网 IP）。([RustDesk][1])

---

# 一、准备工作（假设）

* 主机：Linux（Ubuntu/CentOS 任意发行版均可），有公网 IP（例如 `203.0.113.45`），且能在云控制台或路由器打开端口。
* 已安装 Docker（若未装：`curl -fsSL https://get.docker.com | sh`）。
* 端口/协议：需要在服务器上打开并允许入站 TCP `21114-21119` 和 UDP `21116`（最少需要 `21115-21117` + UDP 21116；官方建议开放 `21114-21119`）。([RustDesk][1])

---

# 二、端口与概念快速说明（关键）

* `hbbs` = ID / 信令（rendezvous）服务器，**默认监听** 包括 TCP `21115/21116/21118`（及 API 21114 在 Pro 场景）与 **UDP 21116**（NAT 打洞）。
* `hbbr` = Relay（中继）服务器，**默认监听** TCP `21117`（以及 `21119` 为 web-socket）。
* 客户端通常需要把 **ID Server**（hbbs）填为 `公网IP:21116` 或 `公网IP`（若用默认端口），若你运行了单机一套服务，客户端也会自动推断 relay（hbbr）默认端口 `21117`，但可手动填 `公网IP:21117`。([RustDesk][1])

---

# 三、下载镜像并用 `docker run` 启动（推荐做法：host 网络）

> 推荐在 Linux 上使用 `--net=host`（host 网络模式），这样容器使用宿主机的网络，避免端口映射复杂、能正确看到真实客户端 IP，且是官方示例推荐方式（若在 Windows Docker 或某些场景不能用 host 模式，需使用 `-p` 明确映射端口并注意 NAT）。([RustDesk][2])

1. 拉取镜像：

```bash
sudo docker pull rustdesk/rustdesk-server:latest
```

2. 在一个目录（例如 `/opt/rustdesk`）创建数据目录并进入：

```bash
sudo mkdir -p /opt/rustdesk/data
cd /opt/rustdesk
```

3. 启动 hbbs（信令/ID 服务）：

```bash
sudo docker run -d --name hbbs \
  -v "$(pwd)/data":/root \
  --restart unless-stopped \
  --net=host \
  rustdesk/rustdesk-server:latest hbbs
```

4. 启动 hbbr（relay 中继）：

```bash
sudo docker run -d --name hbbr \
  -v "$(pwd)/data":/root \
  --restart unless-stopped \
  --net=host \
  rustdesk/rustdesk-server:latest hbbr
```

* 上述命令会把数据持久化到 `./data`，首次运行会在该目录生成密钥文件（`id_ed25519` / `id_ed25519.pub` 等）。
* 如果你的宿主机是 Windows 或 Docker Desktop，`--net=host` 可能不可用，这种情况下需用 `-p` 明确映射多个 TCP/UDP 端口（更复杂且容易出错），优先在 Linux 上用 host 模式。([RustDesk][2])

---

# 四、使用 `docker-compose`（可选，便于管理）

将下面 `docker-compose.yml` 放到 `/opt/rustdesk/`，然后 `docker compose up -d`：

```yaml
version: "3.8"
services:
  hbbs:
    image: rustdesk/rustdesk-server:latest
    container_name: hbbs
    command: hbbs
    volumes:
      - ./data:/root
    network_mode: "host"
    restart: unless-stopped

  hbbr:
    image: rustdesk/rustdesk-server:latest
    container_name: hbbr
    command: hbbr
    volumes:
      - ./data:/root
    network_mode: "host"
    restart: unless-stopped
```

> 注意：`network_mode: "host"` 在 Docker Compose 下同样有效，但仅在 Linux 主机上工作。([RustDesk][3])

---

# 五、防火墙 & 云安全组（非常重要）

在服务器上打开必要端口（以 Ubuntu + ufw 为例）：

```bash
# 允许 SSH（如果需要）
sudo ufw allow ssh

# TCP: 21114-21119
sudo ufw allow 21114:21119/tcp

# UDP: 21116
sudo ufw allow 21116/udp

sudo ufw enable
sudo ufw status
```

如果你在云供应商（AWS/GCP/DO/Azure）上，也要在云控制台的安全组或防火墙规则中开放相同端口（入站规则）。官方建议至少开放 TCP `21115-21117` 与 UDP `21116`，更稳妥则开放 `21114-21119`。([RustDesk][1])

---

# 六、取出公钥并在客户端配置（重要：保证加密通讯）

1. 查看生成的公钥文件（容器内或宿主数据目录）：

```bash
# 如果用上面挂载 data 到宿主目录：
cat /opt/rustdesk/data/id_ed25519.pub
```

2. 客户端配置（RustDesk 桌面客户端）：

   * 打开 RustDesk → 设置 → **Client Configuration**。
   * 在 **ID Server** 填写：`203.0.113.45:21116`（替换为你的公网 IP 与端口，如果使用默认端口 `21116`，写 `IP:21116` 更明确）。
   * 在 **Relay Server**（若需要）填：`203.0.113.45:21117`。
   * **Key**：把上面 `id_ed25519.pub` 的内容粘到客户端的 Key 栏（这让客户端与服务器建立加密信任）。([RustDesk][4])

> 关键：没有把 `id_ed25519.pub` 的公钥放到客户端 Key 栏，会出现 `Key mismatch` / `Not ready` 等错误。([RustDesk][5])

---

# 七、测试连接流程

1. 从一台外部机器（非同一 LAN）安装 RustDesk 客户端，设置 ID Server / Relay Server / Key 如上。
2. 在另一台被控机器上启动 RustDesk，被控端会显示 ID/密码。
3. 在控制端输入被控端 ID 连接，观察日志与连接是否通过 P2P（直连）或通过中继（hbbr）。

调试命令（查看容器日志）：

```bash
sudo docker logs -f hbbs
sudo docker logs -f hbbr
```

如果有 `Listening on tcp :21117` 或类似日志，说明服务已监听并就绪。([RustDesk][6])

---

# 八、常见问题与排查（实战要点）

1. **无法连接 / 客户端显示 `Not ready`**

   * 检查防火墙是否允许 TCP `21114-21119` 与 UDP `21116`。([RustDesk][5])
   * 确认 `hbbs`/`hbbr` 正在运行：`docker ps` / `docker logs`。([RustDesk][2])

2. **为什么要 `--net=host`？**

   * `--net=host` 让容器看到真实公网 IP 并接收 UDP 包（对于 NAT 打洞很关键）。官方示例与社区也推荐在 Linux 上使用 host 模式。若不能使用 host，则需把所有相关 TCP/UDP 端口逐一映射且更复杂。([RustDesk][2])

3. **没有域名能否用 IP？**

   * 可以直接用公网 IP + 端口（例如 `203.0.113.45:21116`）。唯一额外步骤是把 `id_ed25519.pub` 的 Key 填入客户端，确保加密认证通过。([RustDesk][4])

4. **中继和信令可以放在不同机器吗？**

   * 可以，但如果 `hbbr`（relay）不在 `hbbs` 同机或不是默认端口，需要在 hbbs 配置 `RELAY_SERVERS` 并在客户端显式配置 relay 地址。大多数单机部署直接同时运行 hbbs 与 hbbr 最简单且稳定。([RustDesk][6])

5. **尝试用标准端口（80/443）？**

   * 社区反馈很多不成功，原因是 RustDesk 使用一组连续端口和 UDP 打洞，随意映射到 80/443 常会失败。若确实需要走 443，需深入调整并非推荐方案。([GitHub][7])

---

# 九、增强与安全建议

* 把 `./data` 做定期备份（内含私钥/数据库）。
* 若担心中间人或泄露，可在宿主机上做额外防火墙限制，仅允许特定 IP 段入站（例如只允许你办公网段）。([RustDesk][6])
* 若需要 Web UI / 管理界面或 Pro 功能（API/License），参看官方 Pro 文档并把 `API` 端口（21114）适当公开或代理到 HTTPS。([RustDesk][3])

---

# 十、完整快速复查清单（Checklist）

1. [ ] Docker 已安装并能运行容器。
2. [ ] 拉取 `rustdesk/rustdesk-server:latest`。
3. [ ] 启动 `hbbs` 与 `hbbr`（建议 `--net=host`）。([RustDesk][2])
4. [ ] 开放防火墙：TCP `21114-21119`、UDP `21116`（并在云控制台打开相同端口）。([RustDesk][1])
5. [ ] 读取 `id_ed25519.pub`，把公钥粘到客户端 Key。([RustDesk][4])
6. [ ] 客户端 ID Server 填 `公网IP:21116`，Relay 如需填 `公网IP:21117`。([RustDesk][4])

---

如果你愿意，我可以继续帮你做下面任一件事（选一项我会直接给出可执行命令或文件）：

1. 把上面的步骤写成一个 **一键部署脚本**（Bash），自动安装 Docker、拉镜像、生成 `docker-compose.yml` 并启动。
2. 给你一份完整的 `docker-compose.yml`（含环境变量示例，例如 `ALWAYS_USE_RELAY`、自定义端口等）。
3. 帮你写一个小脚本从容器目录拿出 `id_ed25519.pub` 并把它格式化成客户端可直接粘贴的字符串。
4. 针对你具体的云供应商（AWS / DO / GCP）给出安全组 / 防火墙具体操作指引。

你想先做哪一项？（选数字）

[1]: https://rustdesk.com/docs/en/self-host/?utm_source=chatgpt.com "Self-host – Documentation for RustDesk"
[2]: https://rustdesk.com/docs/en/self-host/rustdesk-server-oss/docker/?utm_source=chatgpt.com "Docker – Documentation for RustDesk"
[3]: https://rustdesk.com/docs/en/self-host/rustdesk-server-pro/installscript/docker/?utm_source=chatgpt.com "Docker – Documentation for RustDesk"
[4]: https://rustdesk.com/docs/en/self-host/client-configuration/?utm_source=chatgpt.com "Client Configuration"
[5]: https://rustdesk.com/docs/en/self-host/rustdesk-server-pro/faq/?utm_source=chatgpt.com "FAQ – Documentation for RustDesk"
[6]: https://rustdesk.com/docs/en/self-host/rustdesk-server-pro/relay/?utm_source=chatgpt.com "Configure Relay Servers"
[7]: https://github.com/rustdesk/rustdesk/discussions/984?utm_source=chatgpt.com "Use standard ports 80, 443? #984 - rustdesk ..."
